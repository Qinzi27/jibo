<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="app.leancrew.local" android:versionCode="6" android:versionName="1.4.0">
    <uses-sdk android:minSdkVersion="26" android:targetSdkVersion="35" />
    <!-- Deliberately NO INTERNET, storage, notification, camera, or microphone permissions. -->
    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:theme="@style/AppTheme"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:usesCleartextTraffic="false"
        android:hardwareAccelerated="true"
        android:debuggable="false"
        android:enableOnBackInvokedCallback="true">
        <provider android:name=".CameraFileProvider"
            android:authorities="app.leancrew.local.camera"
            android:exported="false"
            android:grantUriPermissions="true" />
        <activity android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
